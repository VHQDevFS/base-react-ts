import type { ComponentStyleConfig } from '@chakra-ui/react';

const Menu: ComponentStyleConfig = {
  baseStyle: {
    menu: {},
    item: {
      w: '100px',
      // or
      maxW: '100px',
    },
  },
};

export default Menu;
